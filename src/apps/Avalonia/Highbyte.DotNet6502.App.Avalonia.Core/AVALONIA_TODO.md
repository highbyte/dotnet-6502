Audio
- Fix to instantly stop all audio playback when stopping the emulator. Currently in the NAudio WavePlayer for browser will still loop the last buffer for a short while after stopping. Fix in JavaScript module?
- Fix volume not being remebered from when emulator is running. If the volume changed while running, it will be back to default next time running (the slider shows wrong).

Control C64 joystick with Controller
- Is there built-in support in Avalonia for both desktop and browser?